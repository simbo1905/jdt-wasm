[package]
name = "jdt-wasm-transformer"
version = "0.1.0"
edition = "2021"
description = "Example: JDT transform compiled to Rust, built as WASM (placeholder)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
serde_json = "1"
jdt-codegen = { path = "../jdt-codegen" }
